version: v1
managed:
  enabled: true
  go_package_prefix:
    default: github.com/fbriansyah/go-payment-gateway/internal/am
    except:
      - buf.build/googleapis/googleapis
plugins:
  - name: go
    out: .
    opt:
      - paths=source_relative
